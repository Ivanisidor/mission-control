# Mission Control production env (LAN-only)
# Copy to .env.production and fill in

PORT=3000
HOSTNAME=0.0.0.0

# Convex (hosted) deployment URL
NEXT_PUBLIC_CONVEX_URL=
CONVEX_URL=
NEXT_PUBLIC_CONVEX_SITE_URL=

# Ingest auth
ACTIVITY_INGEST_TOKEN=

# Where scripts/log.mjs should send ingest events (this server)
MISSION_CONTROL_BASE_URL=http://127.0.0.1:3000
